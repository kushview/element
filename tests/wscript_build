#!/usr/bin/env python

bld.program(
    source = bld.path.ant_glob("**/*.cpp"),
    includes = ['.', '../project/Source'],
    target = 'tests',
    use = 'JUCE_CORE',
    install_path = None
)
